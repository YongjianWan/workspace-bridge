# workspace-bridge 使用证明与影响力证明

> ⚠️ **DEPRECATED — 本文件数据已过时（2026-04-29）**。当前仓库结构与健康度已大幅变化。请运行 `npm run self-audit` 获取最新数据，或查看 [AGENTS.md §项目骨架](../AGENTS.md#项目骨架自分析结果)。

## 项目地址

https://github.com/YongjianWan/workspace-bridge

---

## 1. 项目健康度全景（audit-summary）

```bash
$ node cli.js audit-summary --cwd . --json --quiet
```

**关键结果：**
- 仓库规模：70 文件，69 主代码 + 1 参考
- 健康评分：5/5（README、LICENSE、gitignore、editorconfig、CI 均到位）
- 死导出：0
- 未解析 import：0
- 循环依赖：0
- 验证框架：已识别 custom-node-scripts

```json
{
  "ok": true,
  "healthScore": "5/5",
  "summary": {
    "severity": "medium",
    "counts": {
      "deadExports": 0,
      "unresolved": 0,
      "cycles": 0
    }
  }
}
```

---

## 2. 项目热区与核心模块识别（audit-overview）

```bash
$ node cli.js audit-overview --cwd . --json --quiet
```

**关键结果：**
- 识别出 12 个热区文件（高风险改动区域）
- 识别出 7 个孤儿文件（可能未使用）
- 核心模块按依赖中心性排序：
  1. `src/utils/path.js` — 被 14 个模块依赖
  2. `src/services/dep-graph.js` — 被 9 个模块依赖
  3. `src/utils/stack-detector.js` — 被 5 个模块依赖
  4. `src/utils/command.js` — 被 5 个模块依赖

```json
{
  "ok": true,
  "summary": {
    "severity": "low",
    "insights": [
      "发现 12 个热区文件，需要重点关注",
      "发现 7 个孤儿文件（可能未使用）"
    ],
    "recommendations": [
      "优先审查热区文件: cli.js, test/functionality-test.js, src/cli/formatters/",
      "审查孤儿模块是否可删除: benchmark/compare.js",
      "高耦合模块拆分优先级: src/utils/path.js, src/services/dep-graph.js"
    ]
  }
}
```

---

## 3. 死代码检测（dead-exports）

```bash
$ node cli.js dead-exports --cwd . --json --quiet
```

**关键结果：**
- 当前无死导出（上次运行发现的 `src/utils/logger.js` 已于 2026-04-30 删除）

```json
{
  "ok": true,
  "deadExportCount": 0,
  "deadExports": []
}
```

---

## 4. 单文件影响半径（impact + audit-file）

### 4.1 底层基础设施改动风险

```bash
$ node cli.js audit-file --cwd . --file src/utils/path.js --json --quiet
```

**关键结果：**
- `path.js` 被 14 个模块直接依赖
- 改动将影响 **32 个文件**（含传递依赖）
- 波及 **17 个测试文件**
- 风险评级：high

```json
{
  "ok": true,
  "file": "src/utils/path.js",
  "summary": {
    "severity": "high",
    "counts": {
      "impact": 32,
      "affectedTests": 17
    }
  }
}
```

### 4.2 核心引擎改动风险

```bash
$ node cli.js impact --cwd . --file src/services/dep-graph.js --json --quiet
```

**关键结果：**
- `dep-graph.js` 导出 `DependencyGraph` 类
- 被 9 个模块直接 import
- 改动将影响 **11 个文件**
- 含传递依赖：`cli.js`（通过 `container.js` 间接依赖）

```json
{
  "ok": true,
  "file": "src/services/dep-graph.js",
  "impactCount": 11,
  "symbolImpact": {
    "mode": "symbol",
    "sourceSymbols": ["DependencyGraph"],
    "directCount": 9
  }
}
```

---

## 5. 能力总结

| 能力 | 证明命令 | 结果 |
|------|----------|------|
| 项目健康度扫描 | `audit-summary` | 5/5 分，0 循环依赖 |
| 热区/孤儿检测 | `audit-overview` | 12 热区，7 孤儿 |
| 死代码检测 | `dead-exports` | 0 个死导出 |
| 变更影响半径 | `impact` / `audit-file` | path.js → 32 文件/17 测试 |
| 符号级影响 | `impact --file` | `DependencyGraph` 类 9 个直接依赖者 |
| 技术栈自动识别 | `audit-summary` | 识别 npm + custom-node-scripts |

---

*Generated by workspace-bridge self-audit on 2026-04-29*
