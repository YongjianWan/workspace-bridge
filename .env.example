# Workspace Bridge Environment Configuration

# Debug mode - set to '1' or 'true' to enable verbose logging
# DEBUG=1

# Override workspace root detection
# WORKSPACE_ROOT=/path/to/your/project

# CLI Fallback Configuration
# WB_GLOBAL_CLI=workspace-bridge-cli
# WB_FORCE_LOCAL=1
